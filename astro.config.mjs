import { defineConfig } from 'astro/config';

// Drives the canonical URL and the machine-readable provenance links (Blog Charter §5).
export default defineConfig({
  site: 'https://inthemarkdowns.com',
});

import { defineConfig } from 'astro/config';

// Placeholder until the domain is registered — one line to change on launch. Drives the
// canonical URL and the machine-readable provenance links (Blog Charter §5).
export default defineConfig({
  site: 'https://ariablog.au',
});

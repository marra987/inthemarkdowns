# Copied to .env for local `npm run build`/`npm run dev`. Astro exposes PUBLIC_-prefixed vars
# to client-side code, so this is the Turnstile *site* key only — never the secret key.
PUBLIC_TURNSTILE_SITE_KEY=
